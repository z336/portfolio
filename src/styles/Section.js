import styled from 'styled-components';

const Section = styled.section`
  max-width: 125ch;
  margin: 0 auto;
  padding: 3rem 1rem;
  a:hover {
    color: var(--red);
  }
`;

export default Section;
